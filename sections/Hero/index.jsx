import styles from './hero.module.scss'
import {
  Alert,
  AlertIcon,
  AlertTitle,
  AlertDescription,
  CloseButton
} from "@chakra-ui/react"

const Hero = ({ data }) => {

  return <div className={`${styles.hero}`}>

  </div>
}

export default Hero;
